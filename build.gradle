plugins {
    id 'java'
    id 'application'
}

group = 'org.DVM'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

application {
    mainClass = 'org.DVM.Main' // 여기에 실제 메인 클래스의 경로를 작성합니다.
}

test {
    useJUnitPlatform()
}